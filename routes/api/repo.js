var Routes = []
/*

var spaceCreate = (request, reply) => {
  // Create a new space from the model.
  var space = new Models.Space({
    name: '',
    users: [],
    created: Date.now()
  })

  // Save the space.
  space.save((err, spaceDoc) => {
    // Tell the user if there's an error saving and quit.
    if (err) {
      reply({
        error: "There was an error saving your space to the DB."
      })
      return
    }

    reply({
      space: spaceDoc._id
    })
  })
}

var repoDelete = (request, reply) => {

}



Routes.push({
  method: 'POST',
  path: '/space/create'
  handler: spaceCreate
})*/

module.exports = Routes